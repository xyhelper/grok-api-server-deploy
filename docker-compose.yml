services:
  backend:
    image: ghcr.io/xyhelper/grok-api-server:latest
    ports:
      - 8500:8001
    environment:
      - TZ=Asia/Shanghai # 指定时区
      - GROKPROXY=  # 接入网关地址
      - FILESERVERPREFIX=http://1.2.3.4:8500 # 文件服务地址,配置为grok-api-server可被外部访问的地址
      - FILEDOWNLOADPROXY=                  # 文件下载代理地址,不需要可留空
      - S3REGION=                           # S3存储区域（AWS S3填写对应区域代码，Cloudflare R2填写 auto）,不需要可留空
      - S3ENDPOINT=                         # S3服务端点地址（使用非AWS的S3兼容存储时需填写，AWS S3留空即可）,不需要可留空
      - S3ACCESSKEYID=                      # S3访问密钥ID（用于身份认证）,不需要可留空
      - S3KEYSECRET=                        # S3访问密钥Secret，与S3ACCESSKEYID配合使用,不需要可留空
      - S3BUCKETNAME=                       # S3存储桶名称,不需要可留空
      - S3URL=                              # S3文件访问的公开URL前缀，用于生成文件的访问链接,不需要可留空
    volumes:
      - ./backend/manifest:/app/manifest
      - ./config/config.yaml:/app/config.yaml
    restart: unless-stopped
   
  mysql:
    image: mysql:8
    command: --mysql-native-password=ON --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - ./data/mysql/:/var/lib/mysql/
      - ./docker-entrypoint-initdb.d/:/docker-entrypoint-initdb.d/
    environment:
      TZ: Asia/Shanghai # 指定时区
      MYSQL_ROOT_PASSWORD: "123456" # 配置root用户密码
      MYSQL_DATABASE: "cool" # 业务库名称
      MYSQL_USER: "cool" # 业务库用户名
      MYSQL_PASSWORD: "123123" # 业务库密码
    restart: unless-stopped
  
  redis:
    image: redis
    # command: --requirepass "12345678" # redis库密码,不需要密码注释本行
    restart: always
    environment:
      TZ: Asia/Shanghai # 指定时区
    volumes:
      - ./data/redis/:/data/